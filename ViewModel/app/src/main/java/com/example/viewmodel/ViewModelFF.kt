package com.example.viewmodel

import android.util.Log
import androidx.lifecycle.ViewModel

class ViewModelFF(num : Int) : ViewModel() {

    init{
        Log.d("ViewModelFF",num.toString())
    }

    // Repository
    // 네트워크 통신

    // Local DB
}